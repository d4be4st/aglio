// Generated by CoffeeScript 1.12.7
(function() {
  var defaultValue, expandDS;

  defaultValue = function(type) {
    switch (type) {
      case 'boolean':
        return true;
      case 'number':
        return 1;
      case 'string':
        return 'Hello, world!';
    }
  };

  module.exports = expandDS = function(root, dataStructures) {
    var i, index, item, j, keyOfArray, len, len1, newValue, ref, ref1, ref2, valueOfArray, valueOfContent;
    switch (root.element) {
      case 'boolean':
      case 'string':
      case 'number':
      case 'enum':
        break;
      case 'member':
        valueOfContent = root['content']['value'];
        newValue = expandDS(valueOfContent, dataStructures);
        root.content['value'] = newValue;
        break;
      case 'object':
        ref1 = root.content;
        for (index = i = 0, len = ref1.length; i < len; index = ++i) {
          item = ref1[index];
          root.content[index] = expandDS(item, dataStructures);
        }
        break;
      case 'array':
        ref2 = root.content;
        for (keyOfArray = j = 0, len1 = ref2.length; j < len1; keyOfArray = ++j) {
          valueOfArray = ref2[keyOfArray];
          newValue = expandDS(valueOfArray, dataStructures);
          root.content = [];
          root.content[keyOfArray] = newValue;
          break;
        }
        break;
      default:
        ref = dataStructures[root.element];
        if (ref) {
          root = expandDS(ref, dataStructures);
        }
    }
    return root;
  };

}).call(this);
